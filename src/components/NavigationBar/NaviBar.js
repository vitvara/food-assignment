import React from "react";
import classes from "./NaviBar.module.css";

const NaviBar = () => {
  return (
    <div className={classes.navi}>
      <h2 className={classes.title}>COOKING MASTER</h2>
    </div>
  );
};

export default NaviBar;
